# TODO — Pixel Art Editor UI

## Phase 1 — Setup
- [x] Create `pixel-art-editor.pen` file
- [x] Get style guide tags
- [x] Apply dark editor style guide
- [x] Set global color variables (bg-base, bg-surface, accent, text, border...)

## Phase 2 — Main Editor (Screen 1)
- [x] Top navigation bar (logo, file/edit/view menus, zoom, canvas info)
- [x] Left tool panel (pencil, eraser, fill, eyedropper, select, line, rect, ellipse, pan, zoom)
- [x] Right properties panel — color section (primary/secondary swatches, HSB/RGB/HEX inputs, opacity)
- [x] Right properties panel — palette section (swatch grid, preset dropdown, add/remove)
- [x] Canvas area (checkerboard bg, pixel grid, rulers, canvas boundary)
- [x] Animation timeline strip (frame thumbnails, FPS, play/pause/stop/loop)
- [x] Status bar (tool name, cursor coords, canvas size, zoom, hex readout)

## Phase 3 — Modals
- [x] New Project Modal (name, width/height, preset sizes, background color, Create/Cancel)
- [x] Export Modal (format tabs PNG/GIF/SVG, scale 1x–8x, preview, filename, Export button)
- [x] Palette Manager (palette list, import button, built-in browser, Apply button)

## Phase 4 — Animation View (Screen 5)
- [x] Animation view variant (expanded timeline, onion skinning toggle, frame count/duration)

## Phase 5 — Validation
- [x] Screenshot each screen
- [x] Check layout alignment with snapshot_layout
- [x] Verify dark theme consistency across all screens
- [x] Fix any spacing/alignment issues (Export modal footer was clipped — fixed)
- [x] Update README screen statuses to done
